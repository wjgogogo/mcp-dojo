{
  "name": "mcp-dojo",
  "private": true,
  "version": "1.0.0",
  "description": "MCP Dojo",
  "scripts": {
    "start:model": "pnpm --filter compatible-server start",
    "start:mcp": "pnpm --filter mcp-server start"
  },
  "dependencies": {
    "prettier": "^3.5.3"
  }
}
